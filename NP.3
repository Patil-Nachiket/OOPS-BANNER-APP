public class OOPSBannerUC3 


    public static void main(String[] args) {
        // Example: Creating a single line for 'O' or 'P' using String.join
        // String.join(delimiter, elements)
        
        // Top/Bottom of O
        String rowFull = String.join("", " ", "O", "O", "O", " ");
        // Middle of O / P
        String rowSides = String.join("", "O", " ", " ", " ", "O");
        // Middle of P / S
        String rowLeft = String.join("", "O", " ", " ", " ", " ");

        // Building the OOPS Banner
        System.out.println("--- UC3: Optimized OOPS Banner ---");
        
        // Example Line 1 (Top of O, O, P, S)
        // Using a space "   " as a delimiter between letters
        String line1 = String.join("   ", rowFull, rowFull, rowFull, rowFull);
        System.out.println(line1);
        
        // Repeat logic for subsequent rows...
    }
}
